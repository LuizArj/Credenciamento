// Supabase client removed — server code now uses Postgres via `lib/config/database.ts`.
// This module is kept as a harmless stub to avoid breaking imports during the migration.
export const supabase = {
    removed: true,
    /* If you need to interact with the database from front-end code, use an API route. */
};